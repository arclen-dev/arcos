#!/usr/bin/env bash
# This script is kept for compatibility.
# Color updates are now handled by hyprlock-gen.sh which reads
# wallust colors directly and regenerates ~/.cache/arcos/hyprlock-dynamic.conf
~/.config/hypr/scripts/hyprlock-gen.sh --now
